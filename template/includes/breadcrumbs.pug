nav(class="breadcrumbs", aria-label="Breadcrumb")
    ul
        each val in breadcrumbs
            if val.url
                li
                    a(href=val.url) !{val.name}
            else
                li !{val.name}
